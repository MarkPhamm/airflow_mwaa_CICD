version: 2

models:
  - name: my_first_model
    description: "A simple example model"
    columns:
      - name: id
        description: "Primary key"
        tests:
          - unique
          - not_null
